

<table>
  <tbody>
    The last upload has been done at <strong><%= @accounting_data.last.created_at %></strong>
    <br>
    <br>
    ALL THE STORED DATA (the first 5 rows only) :
    <br>
    <br>
    <% @accounting_data.each do |data|  %>
      <tr>
        <td><%= data.balance_year %></td>
        <td><%= data.quarter %></td>
        <td><%= data.local_insurer %></td>
        <td><%= data.geo_area %></td>
        <td><%= data.country %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<p><%= link_to "Back to File Upload", new_file_upload_path, class: "btn-yellow" %></p>
